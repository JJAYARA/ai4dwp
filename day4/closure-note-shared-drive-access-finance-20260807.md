Resolved. Cause: `Map-FinBridgeDrives.ps1` was migrated from a user-context GPO logon script to an Intune PowerShell script running as `SYSTEM`, and the mapping logic was not adapted for machine-context startup timing or UNC access readiness, causing the Finance shared-drive mapping to fail at sign-in. Action: Removed the failing `SYSTEM`-context mapping approach, applied the corrected drive-mapping method in the proper execution context, and verified that Finance shared drives were accessible again. Preventive: Require execution-context design review, sign-in timing validation, and pilot rollout checks before moving user-scoped mappings from GPO/user context to Intune or any `SYSTEM`-context deployment model. User confirmed working.